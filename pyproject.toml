[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "fina-maths-lib"
version = "0.1.0"
description = "Deterministic financial maths formula library"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []
authors = [
  { name = "Fina Maths Lib Contributors" }
]
license = { text = "Proprietary" }
keywords = ["finance", "quant", "math", "formulas", "analytics"]
classifiers = [
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3 :: Only",
  "Operating System :: OS Independent",
]

[project.urls]
Homepage = "https://github.com/your-org/fina_maths_lib"
Repository = "https://github.com/your-org/fina_maths_lib"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
